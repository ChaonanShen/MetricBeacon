# Mock scenarios

Fixtures are added and schema-validated in G1. They are owned exclusively by the
assistant-mcp Mock Prometheus Adapter; no frontend, AI Core domain/application package or
MCP handler may read them directly.
